package org.example;

public class Main {
    public static void main(String[] args) {

        String name = "tife alli";
        int sum_of_vowels = 0;

        for(int i = 0; i < name.length(); i++)
        {
            if(name.contains('a') || name.contains("e") || name.contains("i") || name.contains("o") || name.contains("u"))
            {
                System.out.println(3);
            }

        }









    }
}